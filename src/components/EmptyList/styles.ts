import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  wrapper: {
    paddingVertical: 48, 
    alignItems: 'center',
    borderTopWidth: 1,
    borderTopColor: "#333333"
  }, 
  title: {
    marginTop: 16,
    fontSize: 14, 
    fontWeight: 'bold', 
    color: "#808080"
  }, 
  subtitle: {
    fontSize: 14, 
    color: "#808080"
  }
})